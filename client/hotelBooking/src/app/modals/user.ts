export interface IUser{
    userId: string;
    email: string;
    username: string;
    password: string;
    accessToken: string;
}

export type User = {
 
        email: string;
        username: string;
        password: string;
        rePass: string
      
}